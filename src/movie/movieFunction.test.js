const { mockData } = require("../../mock");
const { addMovie, listMovies } = require("./movieFunctions");

describe("addMovie tests", () => {
  it("should add movie and find a movie", async () => {});
});
